---
description: Audit this repo for drift against its own design principles (invokes the repo-health skill).
---

Run a repo-health audit: $ARGUMENTS

Use the `repo-health` skill. Pass `docs`, `permissions`, or `installer` to scope to one check (default: all), and `--fix` to apply mechanical fixes instead of only reporting. Read-only by default.
