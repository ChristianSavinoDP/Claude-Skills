---
description: Gather read-only context for a task from a ticket, PR, repo, or URL (invokes the gather-context skill).
---

Gather context for: $ARGUMENTS

Use the `gather-context` skill. Read-only: accepts a Jira key, a Jira/epic URL, a GitHub PR link, or a repo/file reference. Resolves the chain in both directions and can read repos not cloned locally.
